language: python
python:
  - "3.6"

# command to install dependencies
before_script:
  - pip install -r requirements.txt

env:
  - DJANGO=2.4

services:
  - postgresql

before_install:
  -DJANGO_SETTINGS_MODULE="inventarium.settings.travis"

# command to run tests
script:
  - ./manage.py test
